deck_id = "poc-core-8"
title = "Terminal PoC Deck"
description = "Small deterministic deck for terminal-first validation on macOS before Raspberry Pi migration."

[selection]
target_count = 8
allowed_parts_of_speech = ["noun", "verb", "adjective"]
difficulty_buckets = ["A1", "A2", "B1"]
min_frequency_per_million = 6.0
max_frequency_per_million = 20.0
required_tags = ["poc"]
include_examples = true

